I will fix the breadcrumb visual alignment and refactor its data mapping logic as requested. I will also add the required unit tests.

### 1. Visual Alignment Fix
- **Objective**: Align the breadcrumb top/baseline with the left sidebar title.
- **Approach**: 
  - The Sidebar title uses `text-lg` (line-height ~28px) and is vertically centered in a 64px container.
  - The Breadcrumb is currently vertically centered but may have a different line-height or internal spacing.
  - I will modify `src/components/layout/MainLayout.vue` to ensure the breadcrumb container matches the vertical alignment of the sidebar title.
  - I will use `flex` alignment and explicit `line-height` control to ensure pixel-perfect alignment.

### 2. Breadcrumb Logic Refactoring
- **Objective**: Implement the 3-level hierarchy: Project Name > Level 1 Menu > Level 2 Menu.
- **Approach**:
  - **Menu Structure Definition**: Create a `menuMap` object in `MainLayout.vue` (or a separate composable) that defines the hierarchy of the sidebar menu.
  - **Route Mapping**: Update `src/router/index.ts` to add `meta.activeMenu` to "detail" routes (e.g., `ScriptEditorView` is not in the menu but belongs to `ScriptListView`). This allows mapping hidden routes to their parent menu items.
  - **Breadcrumb Generation**: Implement a computed property `breadcrumbs` that:
    1.  Always starts with the **Project Name** (from `projectStore`).
    2.  Finds the active **Level 1 Menu** item based on the current route.
    3.  Finds the active **Level 2 Menu** item (if applicable).
    4.  Updates dynamically on route changes.
  - **Internationalization**: Ensure text comes from the menu definition (which can be extended for i18n later, currently using the Chinese text as per prototype).

### 3. Testing
- **Objective**: Verify the fixes and logic.
- **Tools**: Install `vitest`, `jsdom`, and `@vue/test-utils` (since they are missing).
- **Unit Tests**: Create `src/components/layout/__tests__/MainLayout.spec.ts` to test:
    - Breadcrumb content for top-level routes.
    - Breadcrumb content for nested/submenu routes.
    - Breadcrumb content for detail pages (verifying the mapping).
    - Project name changes.
- **End-to-End Verification**: Since I cannot run a full GUI browser, I will use the unit tests to simulate "End-to-End" user flows (URL access, navigation) by mounting the component and interacting with the router.

### Implementation Steps
1.  **Install Dependencies**: `npm install -D vitest jsdom @vue/test-utils`.
2.  **Update Router**: Add `meta.activeMenu` to `src/router/index.ts`.
3.  **Refactor MainLayout**:
    -   Define `menuConfig`.
    -   Implement `breadcrumbs` computed logic.
    -   Update template to use `breadcrumbs`.
    -   Apply CSS fixes.
4.  **Add Tests**: Create and run `src/components/layout/__tests__/MainLayout.spec.ts`.
