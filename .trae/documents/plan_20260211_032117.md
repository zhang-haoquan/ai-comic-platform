我将分以下几个步骤完成剧本解析功能的升级，确保生成高质量的模拟数据并优化展示体验：

## 1. 核心逻辑与数据升级 (`src/stores/script.ts`)
重点重构 `parseScript` 方法，实现以下改进：
- **性能优化**：将模拟解析的延迟时间从 2000ms 缩短至 300ms，确保点击“一键解析”后系统能几乎立即响应并跳转。
- **构建高拟真数据模型**：
  - **世界观**：生成包含“背景时代”、“社会规则”、“核心冲突”的结构化描述文本。
  - **角色**：创建包含“姓名、身份、性格特征、人物关系”的详细角色数据。
  - **场景**：生成包含“地点名称、环境描述、氛围基调”的场景数据。
  - **物品**：创建包含“道具名称、功能作用、象征意义”的物品数据。
  - **剧情节奏**：生成标准的“起、承、转、合”四阶段关键事件节点。
  *数据主题将基于现有的“山海经奇缘”进行深度扩展，融合玄幻与人性冲突，确保逻辑自洽。*

## 2. 界面展示优化 (`src/views/script/ScriptParseView.vue`)
调整解析结果页面的 UI 以适应更丰富的数据展示：
- **世界观模块**：调整文本域默认高度，确保能完整展示包含三个维度的长文本。
- **列表卡片优化**：检查角色、场景和物品卡片的排版，优化 `description` 字段的展示逻辑，确保用户能通过滚动或提示看到完整的“人物关系”或“物品象征意义”等详细信息。

## 3. 类型定义检查 (`src/types/index.ts`)
- 快速检查现有类型定义，确认是否需要微调以支持新的数据字段（主要通过复用 `description` 字段来承载复合信息，保持架构稳定性）。

## 4. 验证流程
- 进入 `script/edit/{id}` 界面，点击“一键解析”。
- 验证页面是否在 500ms 内跳转至 `script/parse`。
- 检查五个模块的数据是否完整、逻辑连贯且展示清晰。