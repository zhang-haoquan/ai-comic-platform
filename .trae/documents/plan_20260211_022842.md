# 修改和优化计划

根据您的需求，我制定了以下代码修改计划：

## 1. 剧本解析结果界面优化

**目标**: 点击"确认并生成分镜"后自动跳转到分镜表界面。
**文件**: `src/views/script/ScriptParseView.vue`

### 实施步骤:

1. **引入路由**: 导入 `useRouter` 并初始化 router 实例。
2. **添加跳转逻辑**: 在 `handleConfirm` 函数中，当分镜生成成功并结束 loading 状态后，添加路由跳转代码 `router.push({ name: 'Storyboard' })`。

## 2. 分镜表编辑界面 (镜头详情) 优化

**目标**: 移除操作模块（查看关联剧本段落、查看评论、下载图片、分享等）。
**文件**: `src/views/storyboard/ShotDetailView.vue`

### 实施步骤:

1. **移除 UI 组件**: 删除代码中包含"操作"标题的 `<el-card>` 及其内部的所有按钮（查看关联剧本段落、查看评论、下载图片、分享）。
2. **清理依赖**: 移除不再使用的图标引入（如 `Document`, `ChatDotRound`, `Download`, `Share`）。

***

确认后，我将按照上述计划执行代码修改。
